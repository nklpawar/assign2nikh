apiVersion: v2
kind: Service
metadata:
  name: applicationservicev2
  labels:
    name: appnikhv2
spec:
  type: NodePort
  ports:
  - port: 8081
    targetPort: 8080
    nodePort: 30000
  selector:
    name: appnikhv2